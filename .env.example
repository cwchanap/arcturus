# Better Auth Secret (generate with: openssl rand -base64 32)
# This is REQUIRED for authentication to work
BETTER_AUTH_SECRET=your_better_auth_secret_here

# OAuth Providers (optional - only needed if using social authentication)
# GitHub OAuth App: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth App: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# For local development, these are read from process.env
# For production on Cloudflare Workers, set these as secrets:
#   wrangler secret put BETTER_AUTH_SECRET
#   wrangler secret put GITHUB_CLIENT_ID
#   wrangler secret put GITHUB_CLIENT_SECRET
#   wrangler secret put GOOGLE_CLIENT_ID
#   wrangler secret put GOOGLE_CLIENT_SECRET
