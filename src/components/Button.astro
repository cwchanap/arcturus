---
interface Props {
	variant?: 'primary' | 'outline' | 'ghost';
	href?: string;
	type?: 'button' | 'submit';
	class?: string;
	[key: string]: unknown;
}

const { variant = 'primary', href, type = 'button', class: className = '', ...rest } = Astro.props;

const variantClass =
	variant === 'outline'
		? 'deco-btn-outline'
		: variant === 'ghost'
			? 'deco-btn-ghost'
			: 'deco-btn-primary';

const cls = `deco-btn ${variantClass} ${className}`.trim();
const restProps = rest as Record<string, unknown>;
---

{
	href ? (
		<a href={href} class={cls} {...restProps}>
			<slot />
		</a>
	) : (
		<button type={type} class={cls} {...restProps}>
			<slot />
		</button>
	)
}
